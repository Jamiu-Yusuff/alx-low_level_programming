#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
 * main - two digit combination
 * Return: Always 0 (Success)
 */
int main(void)
{
	int i;
	int j;

	for (i = 0; i < 10; i++)
		for (j = 0; j < 10; j++)
		{
			std : cout << i << j << std :: endl;
		}
	return (0);
}
