#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
 * main - prints from 0 to 10
 * Return: Always 0 (Success)
 */
int main(void)
{
	int i;

	for (i = 0; i <= 9; i++)
	{
		printf("%d\n", i);
	}
	printf("\n");
	return (0);
}
